#include <stdio.h>
#include <string.h>

int main() {
    char sequence[101];
    // int count = 0;
    scanf("%s", sequence);
    // int i = 0;
    printf("%ld", strlen(sequence));

    // printf("%s", sequence);
    // printf("%s %s", sequence[0], sequence[1]);

    // for(int i = 0; i<101; i++);{
    //     printf("%d\n",sequence[i]);
    //     // if(sequence[i] != 0){
    //     //  count = count +1;   
    //     //  printf("%d\n",sequence[i]);
    //     // }
    // }
    // printf("%d", count);
    // printf("%lu", sizeof(sequence));
}